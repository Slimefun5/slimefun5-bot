{
  "name": "slimefun5-bot",
  "version": "1.1.0",
  "description": "Slimefun5 community Discord bot: bug-report relay + slash commands (Cloudflare Worker, no VM) and a Node gateway entry that adds live message filters when run on a VM.",
  "private": true,
  "type": "module",
  "license": "GPL-3.0-or-later",
  "main": "gateway.js",
  "scripts": {
    "start": "node gateway.js",
    "register": "node register.js",
    "deploy": "wrangler deploy",
    "dev": "wrangler dev"
  },
  "dependencies": {
    "discord.js": "^14"
  },
  "devDependencies": {
    "wrangler": "^3"
  }
}
